{
  "name": "axiom",
  "version": "1.0.0",
  "description": "Somnia Data Streams + Reactivity contracts for Axiom",
  "scripts": {
    "compile":       "hardhat compile",
    "ds:setup":      "node scripts/ds_publish.js setup",
    "deploy:infra":  "hardhat run scripts/deploy_infra.js --network somnia"
  },
  "dependencies": {
    "@somnia-chain/streams": "latest",
    "dotenv": "^16.4.5",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@somnia-chain/reactivity-contracts": "latest",
    "hardhat": "^2.22.0"
  }
}
